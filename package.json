{
  "name": "poke-idle-multi-browser",
  "productName": "Poke Idle Multi-Browser",
  "version": "1.0.0",
  "description": "Multi-browser com HUD e recomendador de hunts PIWTools",
  "main": "main.js",
  "scripts": {
    "start": "electron .",
    "package": "electron-packager . \"Poke Idle Multi-Browser\" --platform=win32 --arch=x64 --out=dist --overwrite"
  },
  "devDependencies": {
    "@electron/packager": "^18.3.6",
    "cross-env": "^7.0.3",
    "electron": "^33.0.0"
  }
}
